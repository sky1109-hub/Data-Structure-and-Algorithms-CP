#include <bits/stdc++.h>
using namespace std;
using ll=long long;
int main(){
  ll n,m;cin>>n>>m;
  vector<ll>a(n);
  for(int i=0;i<n;i++)  cin>>a[i];
  ll l=0,h=n,ans=0;
  while(l<=h){
    ll mid=l+(h-l)/2;
    
    
  }
return 0;
}