#include <bits/stdc++.h>
using namespace std;
int solve(int ms,vector<int>&a,int idx){
    if(idx==0)  return 1;
    if(a[idx]==0){
        for(int x=1;x<=ms;x++){
            if()
        }
    }
}
int main()
{
    int n, ms;
    cin >> n >> ms;
    vector<int> a(n);
    for (int i = 0; i < n; i++)
        cin >> a[i];
    cout<<solve(ms,a,n-1);    
    return 0;
}